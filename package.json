{
  "name": "excenor-ai-maturity-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static POC for Excenor Global AI Maturity Scoring.",
  "scripts": {
    "start": "npx serve .",
    "check": "node --check app.js && node --check api/excenor-knowledge.js && node --check api/chat.js && node --check api/leads.js && node --check api/dmaic-agent.js && node --check api/process-diagnostic.js && node --check intelligence/dmaic-agent.js && node --check intelligence/process-intelligence-agent.js"
  },
  "dependencies": {
    "@google/genai": "^1.0.0"
  }
}
